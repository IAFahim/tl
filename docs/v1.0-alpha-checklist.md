# v1.0.0-alpha.2 completion checklist

Base: v1.0.0-alpha.1 at `5c05aa5`. Target: `v1.0.0-alpha.2`.

This checklist records the release boundary. Completed items have executable evidence. Deferred items are named alpha limits rather than unfinished claims. The full design reasoning and future optimization matrix remain in [plan.md](../plan.md).

## Public model

- [x] Replace homogeneous `Timeline<TTrack,TClip>` with non-generic `ITimeline` declarations and `ushort` runtime IDs.
- [x] Allow different closed track/clip kinds inside one timeline.
- [x] Replace `Tracks<TTrack,TClip>` consumption with one `Frame<TTrack,TClip>` callback per active authored track.
- [x] Keep `Frame` to borrowed `Track` and resolved `Clip`, game tick, timeline tick, signed cycle, flags, derived direction, and stable 16-bit `TrackIndex`.
- [x] Remove ordinal and active-count data from the public frame.
- [x] Infer any finite callback parameter list from explicit `in`, `ref`, and `out` modes.
- [x] Generate one stack-only borrowed `Data` context and a dynamic facade adapter.
- [x] Preserve writable caller aliases, including object fields and array elements through compacting GC.
- [x] Provide typed and dynamic signed `TrySeek`, explicit-game-tick start, and idempotent stop.
- [x] Make failure total: invalid ID, owner, lifecycle, tick, or schema returns `false` before user effects.
- [x] Authenticate dynamic playback ownership with a 16-byte readonly sequential `Playback`.

## Authoring and composition

- [x] Discover normal project compile items automatically after a package reference.
- [x] Remove `Build`, `Compile`, `InMemory`, `Bind`, and manual timeline source items.
- [x] Infer track and clip types from `builder.Track` and `builder.Clip`.
- [x] Validate top-level partial timeline declarations, callback shapes, constant payloads, windows, ownership, and overlap bounds.
- [x] Support explicit `Before<THook>` and `After<THook>` phases.
- [x] Support one nested `Include<TTimeline>` chain with deterministic outer-before, included work, outer-after order.
- [x] Diagnose cycles, multiple includes, invalid hooks, missing callbacks, ambiguous slots, and unsupported authoring.
- [x] Preserve authored effect order, including equal-start clips.
- [x] Generate exact-schema direct routes and compatible alternate-schema routes.
- [x] Reject missing or permission-incompatible alternate contexts before effects.
- [x] Keep included definitions and compatible routes limited to definitions visible in the same compilation.
- [x] Document that declarations produced by another source generator cannot feed this pre-compilation pass.

## Generated execution

- [x] Emit unit signed-seek kernels without temporary collections.
- [x] Emit direct per-region basic blocks with fixed track count, payloads, state rules, and blend rules.
- [x] Inline exact-schema public dispatch through the generated context protocol.
- [x] Keep registry, route lookup, compatibility router, and callback calls out of the exact hot body.
- [x] Validate signed delta arithmetic and finite target bounds before effects.
- [x] Replay every crossed local frame in deterministic order.
- [x] Preserve equivalence between a multi-frame delta and repeated signed unit seeks.
- [x] Keep region search logarithmic for random seeking and branch-light for small definitions.
- [x] Allocate 0 managed bytes after warmup in typed and dynamic playback.
- [x] Use no reflection, boxing, delegates, runtime authoring graph, binding table, or managed retained timeline data.
- [x] Store the compact registry and metadata in unmanaged memory.
- [x] Cover all 65,536 timeline IDs without reuse and all 256 module slots.
- [x] Enforce at most 256 closed track/clip kinds and 65,536 authored track instances per definition.
- [x] Preserve deterministic output and timestamps on a cache hit.
- [x] Invalidate generation for source, define, compiler, option, reference, or generator identity changes.
- [x] Run successfully during IDE-shaped design-time compilation with complete metadata references.
- [x] Emit deterministic per-artifact source-size and per-timeline shape reports.
- [x] Expose exact generated static payload bytes and actual retained unmanaged registry bytes.

## Correctness gates

- [x] Rebased C# integration Release and Debug builds have zero warnings and zero errors.
- [x] Run JetBrains Inspect Code 2026.2.1 in Release and classify its 45 findings: 12 namespace-layout conventions, 15 naming conventions, four redundant casts or qualifiers, five constant-flow findings, three receipt/report properties, four marker generic parameters, one record-update style finding, and one test-helper parameter.
- [ ] Resolve or explicitly accept every Release static-analysis finding under issue #22 before publication.
- [x] Core public API approval passes.
- [x] All 96 compiler, C backend, generator, and semantic tests pass on the rebased C# integration.
- [x] Same-compilation compatible routing, missing-schema rejection, include order, hook order, and diagnostics pass.
- [x] Positive/negative seek, looping, gaps, blends, one-tick clips, zero delta, position overflow, and lifecycle receipts pass.
- [x] Managed byrefs remain valid through forced compacting GC.
- [x] Parallel ID and module registration produce unique values.
- [x] ID and module capacity boundaries and overflow rejection pass.
- [x] JIT smoke, mixed sample, and independent benchmark oracle pass.
- [x] NativeAOT smoke, ID capacity, and module capacity pass with identical receipts on the rebased C# integration.
- [x] Generated input/output survive a fresh package-only consumer build.
- [x] Multi-frame positive and negative seeks match repeated unit seeks.
- [x] Invalid finite origins and targets reject before changing playback or outputs.
- [ ] Publish assembly-level line and branch coverage without generated serializer pollution.

## Performance gates

- [x] Measure direct, typed, and dynamic signed seek for literal and runtime deltas.
- [x] Consume complete playback and output receipts so work cannot be removed.
- [x] Verify every benchmark arm against the same independent expected receipt before timing.
- [x] Measure `+1`, `-1`, alternating direction, literal/runtime `+5` and `-5`, repeated unit steps, and a larger replay.
- [x] Run 4,096 logical calls per invocation, 16 warmups, 12 measured iterations, and 250 ms requested iterations.
- [x] Retain three independent Sum processes and three independent Combat processes.
- [x] Keep every generated typed Sum case below 3 ns per executed frame; report Combat separately at 4.165 ns forward and 7.696 ns alternating.
- [x] Normalize multi-frame results as both time per call and time per executed frame.
- [x] Verify 0 B allocation for every measured signed-seek arm.
- [x] Retain full-optimization JIT assembly for the signed-seek matrix.
- [x] Record that process-priority elevation failed, BDN frequency control was unavailable, and PMU collection was pinned to CPU 0.
- [x] Measure signed-seek cycles, instructions, branches, and branch misses with the Linux PMU and make no unsupported NativeAOT timing claim.

## Size and package gates

- [x] Keep the integrated signed-seek candidate at 161,474 production source-and-path bytes with C ABI v2, below 200,000; remeasure the final merged tree.
- [x] Record generated source, runtime assembly, generator package, and NativeAOT executable sizes.
- [x] Pack `Tl.CSharp`, `Tl.Runtime`, `Tl.Gen.CSharp`, `Tl.Compiler`, and `Tl.Gen.C` at `1.0.0-alpha.2`.
- [x] Keep generator and Roslyn assemblies out of application output.
- [x] Prove a second clean package-consumer build reports a generator cache hit.
- [x] Build and run typed and dynamic package-only JIT and NativeAOT consumers from local packages without generator, compiler, or Roslyn output.
- [x] Review package identity, authors, project URL, repository URL, README, and release notes.
- [x] Preserve the repository's current no-license declaration instead of inventing one.
- [ ] Generate `SHA256SUMS` from the exact tagged packages and NativeAOT executables, then attach it with those assets to the GitHub prerelease.
- [ ] Tag the validated commit, push `main`, create the GitHub prerelease, and verify release workflows.

## Compiler ecosystem gates

- [x] Establish `Tl.Compiler` as a C#-free plan package and emit deterministic C ABI v2 signed-seek kernels from `Tl.Gen.C`.
- [x] Make the C backend total for null and overlapping contexts, generated-name collisions, C keywords, standard macros, and reserved identifiers.
- [x] Prove the neutral 16-bit track contract at 255, 256, 65,535, and 65,536 authored instances.
- [x] Qualify signed replay, failure atomicity, 16/40-byte align-8 layouts, playback/output aliasing, and context aliasing with strict cc, gcc, and clang C11 execution.
- [x] Emit deterministic generated source bytes, static-data bytes, runtime-heap bytes, plan format, and ABI version.
- [ ] Finish canonical plan serialization and make both production backends consume one semantic plan.
- [x] Create GitHub issues, labels, milestone, templates, handoff rules, and a release epic.
- [x] Create the GitHub Project and attach every alpha.2 issue.
- [ ] Push the final green C ABI v2 checkpoint and post its validation on issue #16.
- [ ] Build and qualify the Unity ECS/Burst package against a real Unity project.

## Explicit later work

- [ ] Add cross-assembly timeline-schema manifests for `Include` and compatible routing.
- [ ] Integrate with Roslyn generator ordering or move to a compiler host if generated declarations must be inputs.
- [ ] Add cross-definition constant, plan, and kernel interning with exact equality proofs.
- [ ] Add a persistent sequential region cursor only if measurements beat current branch-tree kernels across realistic workloads.
- [ ] Measure NativeAOT throughput with a native-aware harness and controlled CPU environment.
- [ ] Add dynamic-ID benchmark families with at least 16 compatible definitions.
- [ ] Revisit random-seek dispatch using profile-selected trees, dense maps, radix layouts, or SIMD only from measured evidence.
